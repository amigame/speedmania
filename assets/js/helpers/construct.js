construct.configure(function(){
	
	//function 
	//construct.register( construct.input(["mouse","keys"]) );
	construct.register( construct.input(["mouse"]) );
	construct.register( construct.editor([]) );
	
	//construct.configure( input.Keys );
	
});